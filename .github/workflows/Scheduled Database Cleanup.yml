name: Scheduled Database Cleanup

on:
  schedule:
    # Runs at every minute
    - cron: '* * * * *'
  
  # Best Practice: Include this to allow manual testing via the UI
  workflow_dispatch: 

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Run Cleanup Script
        run: echo "Cleaning up the database..."
